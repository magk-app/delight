{
  "name": "delight",
  "version": "0.2.5",
  "description": "AI-powered self-improvement companion platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"cd packages/backend && poetry run uvicorn main:app --reload\"",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "cd packages/backend && poetry run uvicorn main:app --reload",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint && cd packages/backend && poetry run ruff check .",
    "lint:fix": "pnpm --filter frontend lint --fix && cd packages/backend && poetry run ruff check --fix . && poetry run black .",
    "test": "pnpm --filter frontend test && cd packages/backend && poetry run pytest",
    "test:frontend": "pnpm --filter frontend test",
    "test:backend": "cd packages/backend && poetry run pytest",
    "test:e2e": "pnpm --filter frontend test:e2e",
    "test:e2e:ui": "pnpm --filter frontend test:e2e:ui",
    "docs:dev": "cd mintlify && pnpm exec mint dev",
    "docs:lint": "cd mintlify && pnpm exec mint broken-links",
    "docs:deploy": "cd mintlify && pnpm exec mint deploy"
  },
  "dependencies": {
    "mint": "latest"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "packageManager": "pnpm@10.21.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@clerk/shared",
      "sharp",
      "unrs-resolver"
    ]
  }
}
