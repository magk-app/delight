# Clerk Authentication (PUBLIC KEY - safe to expose in browser)
# Get from https://dashboard.clerk.com → Your App → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# API Configuration
# Point to your backend server
NEXT_PUBLIC_API_URL=http://localhost:8000

# SECURITY WARNING: NEVER include CLERK_SECRET_KEY in frontend environment!
# Secret keys belong ONLY in backend environment (packages/backend/.env)
# The publishable key above is designed for client-side use and is safe to expose.

