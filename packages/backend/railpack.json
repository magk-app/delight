{
  "$schema": "https://schema.railpack.com",
  "language": "python",
  "build": {
    "commands": [
      "python -V",
      "pip install --upgrade pip",
      "pip install poetry",
      "poetry export -f requirements.txt --output requirements.txt --without-hashes --without dev"
    ]
  },
  "deploy": {
    "startCommand": "pip install --no-cache-dir -r requirements.txt && python -m alembic upgrade head || true && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
